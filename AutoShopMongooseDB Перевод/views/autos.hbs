<h1>Авто</h1>

{{#if autos.length}}
{{#each autos}}
<div class="row">
  <div class="col s6 offset-s3">
    <div class="fav">
      <div class="fav-image">
        <img src="{{img}}" alt="{{title}}">
      </div>
      <div class="fav-content">
        <span class="fav-title">{{title}}</span>
        <p class="price">{{price}}</p>
      </div>
      <div class="fav-action actions">
        <a id="autosLink"  href="/autos/{{id}}" target="_blank">Открыть авто</a>
        <a id="change"href="/autos/{{id}}/edit?allow=true">Редактировать</a>
        <form action="/fav/add" method="POST">
          <input type="hidden" name="id" value="{{id}}">
          <button id="Buy"  type="submit" class="btn btn-primary">Купить</button>
        </form>
      </div>
    </div>
  </div>
</div>
{{/each}}
{{else}}
<p>Авто пока нет</p>
{{/if}}